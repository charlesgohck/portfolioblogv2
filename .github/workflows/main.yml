name: Deploy Portfolio Blog Main Workflow
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: self-hosted
    steps:
    - name: Checkout Code v3
      uses: actions/checkout@v3
  call-buildanddeploy:
    uses: ./.github/workflows/job2_buildanddeploy.yml
